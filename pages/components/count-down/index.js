var createComponentPage = require('../../../utils/component-page');
Page(createComponentPage({ title: 'CountDown', data: { countPaused: true, countAnimation: 'roll', countStatus: '数字滚动倒计时处于暂停状态。' }, methods: { onToggleCountDown: function () { var paused = !this.data.countPaused; this.setData({ countPaused: paused, countStatus: paused ? '倒计时已暂停。' : '倒计时已经开始。' }); }, onToggleCountAnimation: function () { var animation = this.data.countAnimation === 'roll' ? 'pulse' : 'roll'; this.setData({ countAnimation: animation, countStatus: animation === 'roll' ? '已切换为数字滚动风格。' : '已切换为渐隐入场风格。' }); }, onCountFinish: function () { this.setData({ countPaused: true, countStatus: '等待时间已经结束，可以重新获取验证码。' }); } }}));
