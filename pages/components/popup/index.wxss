@import "../../../styles/component-page.wxss";

.popup-page__placement-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: var(--pui-content-gap);
  width: 100%;
  min-width: 0;
}
