<form
  class="{{rootClass}} {{customClass}}"
  style="{{customStyle}};{{rootStyle}}"
  role="form"
  aria-label="{{ariaLabel || '表单'}}"
  bindsubmit="onNativeSubmit"
  bindreset="onNativeReset"
>
  <slot></slot>
</form>
