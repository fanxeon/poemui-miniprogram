# Lithos Hero

Standalone React 18 + TypeScript + Vite + Tailwind CSS implementation of the Lithos geology hero.

```bash
npm install
npm run dev
```

The cursor-following spotlight is implemented in `src/App.tsx` with a hidden canvas mask applied to the reveal image layer.
